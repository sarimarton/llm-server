# LLM Server Configuration

# Server port
PORT=51732

# Auto-configure Tailscale serve if Tailscale is available
# Set to false to disable automatic Tailscale setup
TAILSCALE_AUTO_SERVE=true

# Claude model (haiku, sonnet, opus)
CLAUDE_MODEL=haiku

# LibreTranslate URL (if using Docker)
LIBRETRANSLATE_URL=http://localhost:5001/translate
